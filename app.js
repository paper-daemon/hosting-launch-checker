const checks=[['HTTPS','https:// で始まっているか'],['Redirect','http→https / www有無が一貫しているか'],['robots.txt','/robots.txt が公開されているか'],['sitemap.xml','/sitemap.xml が公開されているか'],['Headers','HSTS / Content-Type / cache 方針を確認'],['404','存在しないURLで404が返るか']];document.querySelector('#f').addEventListener('submit',e=>{e.preventDefault();const u=document.querySelector('#u').value;document.querySelector('#r').innerHTML=`<h2>${u}</h2>`+checks.map(x=>`<div class="card"><b>${x[0]}</b><p>${x[1]}</p></div>`).join('');renderOffers()});function renderOffers(){const o=(window.AFFILIATE_OFFERS||[]).filter(x=>x.enabled);document.querySelector('#offers').innerHTML=o.length?'<h2>関連サービス</h2>'+o.map(x=>`<div class="card"><a rel="sponsored noopener" target="_blank" href="${x.url}">${x.label}</a><p>広告リンクを含みます。</p></div>`).join(''):''}